// src/graph_models/gql/projection/edge_keep_bitmap.cc
#include "graph_models/gql/projection/edge_keep_bitmap.h"

// Currently all inline in header; this TU reserved for future non-trivial
// methods (e.g., sharded bitmap for MAG240M-scale datasets, serialize to disk).

namespace GQL {}  // intentionally empty
